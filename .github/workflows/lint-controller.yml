name: Lint & Test controller

permissions:
  contents: read
  pull-requests: read

on:
  push:
    branches:
      - main
    paths:
      - "services/dt4mob-controller/**"
      - ".github/workflows/lint-controller.yml"
  pull_request:
    paths:
      - "services/dt4mob-controller/**"
      - ".github/workflows/lint-controller.yml"

jobs:
  build:
    name: Lint
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./services/dt4mob-controller
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: 'services/dt4mob-controller/go.mod'
          cache-dependency-path: 'services/dt4mob-controller/go.sum'

      - name: Run lints
        uses: golangci/golangci-lint-action@v8
        with:
          version: v2.5
          working-directory: ./services/dt4mob-controller

      - name: Run tests
        run: go test -race -vet=off
        if: always()
