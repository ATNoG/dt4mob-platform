# Data Writer

A module that consumes messages from a kafka broker and writtes the events to the available databases like timescale.

## Installation

Use the package manager [uv](https://docs.astral.sh/uv/) to install Data Writer.

```bash
uv sync --locked
```

## Usage

```bash
uv run main.py
```