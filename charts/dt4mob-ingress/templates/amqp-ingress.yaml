{{- if .Values.amqp.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "dt4mob-ingress.fullname" . }}-amqp
  labels:
    {{- include "dt4mob-ingress.labels" . | nindent 4 }}
  annotations:
    nginx.ingress.kubernetes.io/secure-backends: "true"
    nginx.ingress.kubernetes.io/ssl-passthrough: "true"
  {{- with .Values.amqp.annotations }}
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: nginx
  rules:
    - host: {{ .Values.amqp.host | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.amqp.serviceName }}
                port:
                  name: {{ .Values.amqp.port }}
{{- end }}
