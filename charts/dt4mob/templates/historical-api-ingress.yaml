apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "dt4mob.fullname" . }}-historial-api
  labels:
    {{- include "dt4mob.labels" . | nindent 4 }}
spec:
  ingressClassName: {{ .Values.ditto.ingress.className }}
  rules:
  - host: {{ .Values.ditto.ingress.host | quote }}
    http:
      paths:
      - path: /historic
        pathType: Prefix
        backend:
          service:
            name: {{ include "dt4mob.fullname" . }}-historical-api-service
            port:
              name: http
