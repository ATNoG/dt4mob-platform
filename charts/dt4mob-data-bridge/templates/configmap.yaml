apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-config
  labels:
    app.kubernetes.io/name: {{ .Chart.Name }}
data:
  config.toml: |
{{ dict "hono" .Values.hono "devices" .Values.devices | toToml | indent 4 }}
